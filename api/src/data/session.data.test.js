// const { createSession, getSession } = require('./session.data')

describe('Tests for session creation', () => {
  it.todo('Should create a session')
  it.todo('Should not create a session with blank params')
  it.todo('Should not create a session without permissions')
})

describe('Tests for session getting', () => {
  it.todo('Should get a session')
  it.todo('Should not get a session with blank key')
  it.todo('Should not get a session without params')
})
